<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.koreait.hardware.mybatis.mapper.HardwareMapper">
	<select id="selectAll" parameterType="int" resultType="Hardware">
		select * from hardware order by hardware_id=#{hardware_id}
	</select>
</mapper>